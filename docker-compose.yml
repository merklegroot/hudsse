services:
  hudsse-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3005:3002"
    environment:
      - NODE_ENV=production
      - PORT=3002
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
    container_name: hudsse-app
    networks:
      - hudsse-network

networks:
  hudsse-network:
    driver: bridge

